# Source modules

Planned reusable modules:

- `data_collection.py` — retrieve selected World Bank indicators
- `data_cleaning.py` — clean and reshape the downloaded data
- `feature_engineering.py` — create trade openness, lags and the next-year target
- `model_utils.py` — train, tune and evaluate the project models
